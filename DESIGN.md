# cookies Design System

> 设计方向：**Intelligent Blueprint / 智能蓝图**<br>
> 决策日期：2026-07-20<br>
> 适用范围：cookies 桌面版官网、共享产品壳层、四大业务系统及运营后台<br>
> 设计关键词：**创意、简洁、高级感、精确、可信**

## 1. 设计愿景

cookies 是广告增长的智能操作系统。设计系统以钴蓝建立清晰的品牌记忆，以矿物灰承载长时间、高密度的专业工作，以精确的数据表达证明每一次 AI 建议、创意选择和投放动作。

品牌不依赖通用 AI 视觉符号。它通过真实工作流、版本关系、证据、数据和广告作品表达智能。官网让用户看见系统能力，产品工作台让用户专注完成任务。

### 1.1 物理场景

品牌负责人、策略分析师、创意人员和广告投手在白天的办公室内，使用 14 至 27 英寸显示器连续工作。他们需要快速识别当前项目、关键结论、异常风险和下一步动作，同时能随时追溯数据来源与自动化记录。因此，产品默认使用明亮、低眩光的浅色主题；仅媒体预览画布允许切换为局部深色环境。

项目只交付桌面 Web。设计与验收以 1440px 宽视口为基准，最低支持 1280px，不设计手机或平板布局，也不要求页面在低于 1280px 时进行内容重排。

### 1.2 核心原则

1. **蓝色表达结构，不表达装饰**：钴蓝只承担品牌、主操作、当前选择、链接和信息状态。
2. **数据先于装饰**：图表、证据、版本与因果关系是主要视觉内容。
3. **复杂但不混乱**：使用稳定网格、清晰层级和渐进披露，不用卡片堆叠隐藏复杂度。
4. **同源但不同声量**：官网允许大面积品牌蓝；工作台以矿物灰为主，品牌蓝控制在约 10% 以内。
5. **用户始终掌控**：AI 生成、自动执行、费用、风险、来源和人工审批拥有明确且一致的状态语言。
6. **图表清楚且有创意**：先保证数据、坐标、单位、图例和交互清晰，再通过叙事标注、内容关联和品牌化视觉表达形成高级感。
7. **页面克制而有主线**：除信息展示页外，不进行多模块无节制横向平铺；页面必须有一个主任务、一个视觉焦点和明确阅读动线。

## 2. 品牌标识

### 2.1 字标方向

- 主标志使用小写 `cookies` 字标，采用几何无衬线结构。
- 字形应体现精确、开放与亲和，避免过度圆润或儿童化。
- 可从字母连接、节点关系或局部切口中提炼识别点，表达系统连接与持续优化。
- 不使用饼干、咬痕、厨师、机器人、魔法棒或 AI 大脑作为主要识别符号。
- 最终标志需覆盖横版、紧凑图标、单色、反白、16px favicon 与头像场景。

### 2.2 标志使用

- 钴蓝背景使用矿物白字标。
- 浅色背景优先使用深海军蓝字标，钴蓝图标只在需要强化品牌时使用。
- 标志四周最小安全距离为字标中一个 `o` 的宽度。
- 不添加渐变、发光、描边、立体投影或任意拉伸。

## 3. 色彩系统

所有颜色使用 OKLCH 定义。实现阶段允许输出兼容色值，但设计令牌与评审以 OKLCH 为准。

### 3.1 矿物灰

矿物灰带有轻微冷蓝倾向，用于背景、文字、边界和层级。

| Token | OKLCH | 用途 |
| --- | --- | --- |
| `mineral.0` | `oklch(0.988 0.003 250)` | 最高层内容面、反白文字 |
| `mineral.50` | `oklch(0.972 0.006 250)` | 页面背景 |
| `mineral.100` | `oklch(0.946 0.009 250)` | 侧栏、工具栏、悬停面 |
| `mineral.200` | `oklch(0.902 0.012 250)` | 分隔线、禁用背景 |
| `mineral.300` | `oklch(0.830 0.016 250)` | 强边界、占位符 |
| `mineral.500` | `oklch(0.560 0.025 250)` | 次级文字、图标 |
| `mineral.700` | `oklch(0.340 0.030 250)` | 次要标题 |
| `mineral.800` | `oklch(0.265 0.030 250)` | 正文 |
| `mineral.900` | `oklch(0.205 0.028 250)` | 主要标题 |
| `mineral.950` | `oklch(0.145 0.022 250)` | 高对比品牌文字 |

禁止使用纯白与纯黑。默认页面背景为 `mineral.50`，内容面为 `mineral.0`。

### 3.2 钴蓝

| Token | OKLCH | 用途 |
| --- | --- | --- |
| `cobalt.50` | `oklch(0.970 0.020 255)` | 信息浅底、选中浅底 |
| `cobalt.100` | `oklch(0.935 0.045 255)` | 轻量强调 |
| `cobalt.200` | `oklch(0.875 0.085 255)` | 焦点外环浅层 |
| `cobalt.400` | `oklch(0.680 0.200 255)` | 深色背景链接、高亮图形 |
| `cobalt.500` | `oklch(0.580 0.230 255)` | 品牌主色、官网大色块 |
| `cobalt.600` | `oklch(0.500 0.225 257)` | 浅色界面主按钮、选中态 |
| `cobalt.700` | `oklch(0.430 0.195 258)` | 主按钮悬停、文字链接 |
| `cobalt.800` | `oklch(0.360 0.155 260)` | 主按钮按下、深色强调 |
| `cobalt.900` | `oklch(0.300 0.110 262)` | 品牌深色面 |

### 3.3 语义色

| 语义 | 主色 | 浅底 | 使用规则 |
| --- | --- | --- | --- |
| 成功 | `oklch(0.610 0.140 155)` | `oklch(0.955 0.035 155)` | 已通过、已完成、效果改善 |
| 警告 | `oklch(0.720 0.145 78)` | `oklch(0.965 0.040 82)` | 风险、额度接近、等待处理 |
| 危险 | `oklch(0.570 0.205 25)` | `oklch(0.955 0.035 25)` | 失败、超限、不可逆操作 |
| 信息 | `cobalt.600` | `cobalt.50` | AI 建议、系统信息、当前状态 |

状态必须同时包含文字、图标或形状，不允许只通过颜色传达。

### 3.4 数据可视化

默认序列为钴蓝、青绿、琥珀、紫灰、珊瑚红和石板蓝。颜色用于区分数据序列，不继承操作语义。

| 序列 | OKLCH |
| --- | --- |
| Data 1 | `oklch(0.580 0.230 255)` |
| Data 2 | `oklch(0.650 0.125 185)` |
| Data 3 | `oklch(0.740 0.135 82)` |
| Data 4 | `oklch(0.590 0.105 310)` |
| Data 5 | `oklch(0.630 0.155 28)` |
| Data 6 | `oklch(0.520 0.080 225)` |

- 单序列图表只使用钴蓝及其透明度或明度层级。
- 对比实验使用钴蓝表示当前方案，矿物灰表示基线。
- 正负变化同时使用方向符号和数值，不能只使用红绿。
- 图表网格线使用 `mineral.200`，轴文字使用 `mineral.500`。
- 禁止 3D 图表、装饰性渐变面积和没有单位的巨大数字。

#### 创意化表达原则

- 创意表达必须建立在准确编码之上。数值比例、排序、零点、时间间隔、置信区间和坐标尺度不得为了视觉效果被扭曲。
- 每个分析页面选择一个主要图表作为视觉锚点，其余图表作为解释或下钻，不将多个同权重图表平均铺满首屏。
- 优先使用直接标注、关键转折点、基线对比、叙事注释、置信范围和关联素材缩略图，让数据自己形成视觉故事。
- 可将素材帧、受众轮廓、投放阶段或创意结构与图形编码结合，例如视频时间轴与效果曲线同步、素材缩略图与散点直接关联。
- 允许通过留白、局部大比例、线条节奏、裁切和轻量状态动效形成高级感，但同一页面只使用一种主导视觉手法。
- 动效只用于解释数据变化、筛选关系和时间推进，必须可暂停，并支持减少动效偏好。
- 创意图表仍需提供标题、指标定义、时间范围、单位、数据新鲜度、图例或直接标签，以及文字摘要或数据表替代。

#### 禁止项

- 不使用 3D 透视、倾斜坐标、随机曲线、装饰性粒子和无数据含义的面积填充。
- 不为制造戏剧性截断坐标轴、隐藏基线、夸大差异或省略样本与置信提示。
- 不将所有图表套入独立卡片，不在一屏内并列大量小图造成“数据墙”。
- 不使用渐变和高饱和色模拟高级感；颜色必须承担序列、状态、层级或交互含义。

## 4. 字体系统

### 4.1 字体选择

- 中文与产品 UI：MiSans Variable。
- 英文、数字与代码式标识：Geist Sans；等宽数据可使用 Geist Mono。
- 回退字体：`system-ui`, `-apple-system`, `BlinkMacSystemFont`, `Segoe UI`, `sans-serif`。
- 产品控件只使用无衬线字体，不在按钮、标签和表格中使用展示字体。
- 数字默认启用等宽数字特性，保证报表、预算与百分比纵向对齐。

### 4.2 产品字号

| 层级 | 字号 / 行高 | 字重 | 使用场景 |
| --- | --- | --- | --- |
| Display | 32 / 40 | 650 | 工作台欢迎页、关键空态，不用于常规页面 |
| H1 | 24 / 32 | 650 | 页面标题 |
| H2 | 20 / 28 | 600 | 一级内容区标题 |
| H3 | 16 / 24 | 600 | 面板标题、分组标题 |
| Body | 14 / 22 | 400 | 默认正文 |
| Compact | 13 / 20 | 400 | 表格、属性、密集列表 |
| Label | 12 / 18 | 550 | 字段标签、状态、辅助说明 |
| Micro | 11 / 16 | 550 | 时间戳、图表轴、元数据 |

长文本行宽控制在 65 至 75 个字符。产品字号固定，不使用流体缩放。

### 4.3 官网字号

- 首屏中文标题：56/64，字重 650。
- 一级章节标题：40/48，字重 620。
- 正文：18/30。
- 英文标题允许更紧字距，中文正文保持自然字距。
- 官网标题通过尺寸、留白和构图形成品牌感，不使用渐变文字。

## 5. 空间、栅格与形状

### 5.1 间距

基础单位为 4px。标准序列为 `4, 8, 12, 16, 24, 32, 48, 64, 96`。

- 控件内部间距主要使用 8、12、16。
- 内容组间距主要使用 16、24、32。
- 官网章节间距主要使用 64、96。
- 不给所有容器使用相同内边距，按信息层级形成疏密节奏。

### 5.2 官网栅格

- 统一使用 12 列桌面栅格，基准视口 1440px，最大内容宽度 1280px，列间距 24px。
- 1280px 视口使用 32px 页面边距；1440px 及以上使用至少 64px 页面边距。
- 不提供平板和移动端栅格，不为低于 1280px 的视口设计内容重排规则。
- 首屏钴蓝可突破内容容器形成大面积品牌面，文字与操作仍对齐栅格。

### 5.3 产品栅格

- 全局顶栏高度 56px。
- 模块侧栏展开宽度 232px，收起宽度 64px。
- 页面标题区建议 56 至 72px，根据操作数量调整。
- 主内容默认最小边距 24px，宽屏数据页面可使用 32px。
- 1440px 为设计与截图基准；1280px 为最低验收宽度；1680px 以上只扩展内容区或两侧留白，不改变信息结构。
- 侧栏可由用户主动收起，但不因视口宽度自动切换为移动导航。
- 表格、画布和时间线可以占满可用宽度，不强制包裹在卡片中。

### 5.4 页面构图与密度

除信息展示页外，页面默认采用“一个主工作区 + 一个辅助区”的构图。第三个区域仅在任务确实需要实时上下文时出现，并且必须明显弱于主工作区。

| 页面类型 | 推荐结构 | 密度要求 |
| --- | --- | --- |
| 任务与编辑页 | 单一主画布或主流程 + 右侧检查器；列表或版本树按需出现在左侧 | 主工作区至少占内容宽度的 50%；禁止三个以上同权重区域平铺 |
| 对话与文档页 | 主对话或主文档 + 结构化产物；证据作为可收起上下文 | 阅读动线从任务到产物再到证据，不让三栏等宽竞争 |
| 分析页 | 一个主图表或分析矩阵 + 解释区 + 明细下钻 | 首屏只突出一个核心问题，次级图表纵向展开 |
| 信息展示页 | 允许态势、指标、告警和列表并行展示 | 可以较高密度，但必须有主次分区、扫描顺序和统一语义 |
| 作战与监控页 | 主要趋势或状态 + 操作对象表 + 风险与待办 | 高密度仅服务实时判断，异常和主操作优先于完整信息 |

- 普通页面同一横排默认不超过两个主要模块；禁止连续多行使用三至四个同尺寸模块平铺。
- 三栏工作区必须采用明确比例，例如 `56% / 28% / 16%`，不使用三个等宽面板。
- 首屏只设置一个页面级主要操作和一个主要视觉焦点，其余操作进入局部工具栏或渐进披露。
- 优先纵向组织“结论 → 依据 → 明细”，避免为了利用宽屏把所有信息塞在同一水平线上。
- 次级属性、历史、日志、来源和高级设置使用标签、抽屉、可收起检查器或详情下钻，不与主任务争夺首屏。
- 紧凑密度只用于表格、时间线、监控和作战台；表单、对话、创作和评审页面使用默认密度与充足留白。
- 设计评审必须能用一句话说明页面主任务，并能用编号标出用户的第一、第二、第三视觉落点。

### 5.5 桌面支持边界

- 支持桌面版 Chrome、Edge 和 Safari 的当前及前一个主要版本。
- 低于 1280px 的视口不属于产品验收范围，可显示简单的桌面使用提示，不加载另一套页面布局。
- 浏览器缩放以 100% 为基准，在 90% 至 125% 范围内不得遮挡关键操作。
- 创意内容仍可提供手机广告画幅预览，这只是内容模拟，不代表 cookies 页面支持移动端。

### 5.6 圆角、边界与阴影

| Token | 数值 | 用途 |
| --- | --- | --- |
| `radius.sm` | 4px | 标签、紧凑控件 |
| `radius.md` | 6px | 按钮、输入框、菜单 |
| `radius.lg` | 8px | 面板、浮层 |
| `radius.xl` | 12px | 官网重要媒体容器 |

- 胶囊形只用于状态标签、筛选项与极短切换控件。
- 默认边界为 1px `mineral.200`。
- 内容面通常只用边界或背景差建立层级，不同时使用边界和重阴影。
- 浮层阴影：`0 8px 24px oklch(0.205 0.028 250 / 0.10)`。
- 禁止装饰性玻璃模糊、彩色发光和多层重阴影。

## 6. 官网设计规则

### 6.1 色彩策略

官网采用 Committed 策略。首屏或关键章节允许钴蓝覆盖 30% 至 60% 的可视面积，其余区域使用矿物灰和高质量广告作品平衡。每个屏幕只保留一个主要行动。

### 6.2 首页结构

1. 品牌首屏：一句主张、一个主要行动、一个可理解的系统蓝图。
2. 业务闭环：需求、策略、创意、洞察、投放与增长形成连续关系，不做同尺寸功能卡片网格。
3. 四大系统：用真实任务片段和成果证明能力，每一段使用不同的信息结构。
4. 可信证据：案例、效果、来源、审计和安全能力。
5. 转化区域：预约演示、申请试用或查看方案，只突出一个主入口。

### 6.3 图像与图形

- 优先使用真实广告作品、创作过程、数据证据和人物工作场景。
- 系统图采用细线、节点、方向与版本标签，不使用抽象光球和无意义粒子。
- 图片裁切应体现编辑判断，避免通用图库式居中构图。
- 图标使用统一的 1.5px 线性图标，默认 16px 或 20px。

## 7. 产品壳层与四大系统

### 7.1 导航层级

- L0 是 56px 全局顶栏，只包含品牌、组织与项目、系统切换、全局搜索、任务、审批、通知和用户入口。
- L1 是模块自有的 232px 左侧栏，收起后为 64px，承载稳定业务域。
- L2 位于页面标题下方，集合页用于状态或类型切换，详情页用于项目或任务流程。
- L3 只在具体对象或编辑器内部使用局部标签，不创建第三条侧栏。
- 任一页面最多同时显示一条 L1、一条 L2 和一个局部 L3。
- 完整导航树、路由与状态规则见 [四大模块导航与信息架构](./docs/19-module-navigation-architecture.md)。
- 各子板块的必要性、价值、优先级和推荐展示形式见 [四大模块子板块分析](./docs/20-module-submodule-analysis.md)。

### 7.2 共享壳层

全局顶栏只包含 `cookies` 标志、组织与项目切换、全局搜索、任务状态、通知和用户入口。进入业务系统后，由该系统自己的侧栏管理模块导航。

四个系统共享颜色、字体、控件、状态、图标和交互模式，但不共享业务页面布局或业务状态机。

Project 是四系统共同的 L0 上下文。项目切换器提供搜索、最近项目、新建和项目总览；总览以纵向闭环轨道连接需求与策略、创意、投放、洞察和下一轮，当前阶段为主视觉，右侧只放待办、风险和活动。禁止把四个系统做成四张等宽卡片或在项目总览复制模块编辑器。资源关系使用细线、方向、版本标签和清晰深链，符合“精确的数据表达”。

### 7.3 需求与策略

- 代表结构：对话区、结构化 Brief、证据与来源三栏联动。
- 当前提问与待确认字段使用钴蓝；引用证据使用矿物灰和来源编号。
- AI 建议必须显示生成状态、使用模型、引用来源和确认动作。

### 7.4 创意创作

- 图文任务使用画布、结构树、属性区和版本历史。
- 视频创作的 L2 固定分为“品牌广告制作”和“效果广告制作”，不使用一个通用视频编辑器隐藏两类流程。
- 品牌广告以电影感故事为主线，依次呈现故事、剧本、视觉开发、分镜、角色场景、镜头制作、剪辑声音和调色检查。
- 效果广告为功能化工作区，首期分别提供数字人、广告前贴、爆款视频复刻，不让三种功能共用一个参数表单。
- 视频任务使用预览、分镜、时间线、素材带和批注；爆款复刻增加参考结构、原创改写和相似性风险对比。
- 媒体预览区域允许局部深色模式，产品导航与控制区仍遵循主系统。
- 生成中、待审核、已通过和交付失败使用统一语义状态，不靠缩略图外观判断。

### 7.5 素材洞察

- 以筛选器、趋势图、素材矩阵、实验对比和经验结论为主要结构。
- 首屏先回答“发生了什么”和“为什么”，原始明细通过下钻获取。
- 图表与素材缩略图建立直接选择关系，避免将每条洞察做成独立卡片。

### 7.6 智能投放

- 以计划、执行、监控、优化和证据时间线为主要结构。
- 预算、上线、暂停、扩量等真实账户操作使用明确确认页或内联确认，不默认依赖模态框。
- 自动执行必须展示影响范围、预计费用、风险等级、回滚方案和审计记录。
- 异常优先级通过图标、文字、排序和颜色共同表达。

## 8. 组件规则

### 8.1 按钮

- Primary：`cobalt.600` 背景，矿物白文字，用于当前区域唯一主操作。
- Secondary：矿物白背景、`mineral.200` 边界，用于次要操作。
- Tertiary：透明背景，用于工具栏与低优先级操作。
- Danger：仅在明确破坏性动作中使用危险色。
- 常规高度为 36px，紧凑型 30px，大型转化按钮 44px。
- 每个按钮必须覆盖默认、悬停、焦点、按下、禁用和加载状态。

### 8.2 表单

- 标签位于控件上方，帮助文字位于下方，错误信息紧邻字段。
- 默认控件高度 36px，文本域与代码编辑器按内容决定。
- 焦点环使用 2px `cobalt.200` 外环和 1px `cobalt.600` 边界。
- 不使用占位符替代字段标签。

### 8.3 表格与列表

- 默认行高 40px，密集模式 34px。
- 表头保持稳定，数字右对齐，名称和描述左对齐。
- 行操作在悬停或选中时显示，但键盘聚焦时必须同样可见。
- 批量操作进入稳定工具栏，不让页面布局跳动。
- 大数据量使用虚拟滚动、分页或服务端加载，并提供骨架屏。

### 8.4 状态与反馈

- 加载使用贴合内容结构的骨架屏，不在页面中央放孤立转圈。
- 空态说明价值、前置条件和下一步，不只写“暂无数据”。
- 成功反馈轻量且可撤销时优先提供撤销。
- 错误信息说明发生了什么、影响是什么、用户可以做什么。
- 长任务进入全局任务中心，页面内保留状态与结果入口。

### 8.5 浮层

- 菜单、下拉选择、日期选择等使用轻量浮层。
- 详情、筛选和编辑优先使用侧面板或页面内展开。
- 模态框只用于必须中断流程的确认、授权或不可逆操作。
- 不嵌套模态框，不在模态框内再打开复杂编辑器。

## 9. 动效

- 产品状态过渡为 150 至 250ms，使用 ease-out-quart 或 ease-out-quint。
- 官网关键叙事允许 400 至 700ms，但同一屏只保留一个主要动画逻辑。
- 只动画 `transform`、`opacity` 和必要的合成属性，不动画布局尺寸。
- 禁止弹跳、弹性、无限漂浮和装饰性加载编排。
- 支持 `prefers-reduced-motion`，减少模式下移除位移和自动播放。

## 10. 内容与语气

- 文案专业、直接、克制，优先使用广告业务语言，不滥用“赋能”“颠覆”“一键智能”。
- 按钮使用明确动词，例如“生成策略”“提交审核”“暂停计划”。
- AI 结论区分事实、推断与建议，并显示证据来源。
- 危险操作说明真实影响，避免只使用“确定吗”。
- 时间、货币、百分比、时区和平台指标必须明确单位与口径。

## 11. 无障碍与国际化

- 以 WCAG 2.2 AA 为最低目标，正文和关键控件需通过实际渲染后的对比度检查。
- 完整支持键盘导航、可见焦点、语义标签、屏幕阅读器名称和跳转主内容。
- 点击目标常规不小于 40px；密集表格内可在不牺牲可操作性的前提下适当收紧。
- 图表提供文字摘要、数据表或可访问替代内容。
- 中文、英文与数字混排需要测试断行；布局不得依赖固定文案长度。
- 所有日期、货币、数字和时区通过国际化格式化能力输出。

## 12. 禁止项

- 紫蓝渐变、渐变文字、漂浮光球、机器人和 AI 大脑插画。
- 默认玻璃拟态、霓虹发光、赛博终端装饰和全产品暗色化。
- 同尺寸图标卡片网格、卡片套卡片、巨型指标模板。
- 彩色粗侧边线提示、无意义阴影和过量胶囊标签。
- 使用展示字体作为产品控件文字。
- 仅用颜色表示状态，或把低对比度当作高级感。
- 为追求品牌个性重新发明滚动条、表单、导航和标准交互。

## 13. 实施与评审门禁

每个新页面或组件进入开发前，应满足以下条件：

1. 明确属于官网品牌场景还是产品任务场景，并采用对应的色彩声量。
2. 使用本文件的色彩、字体、间距、形状和状态令牌。
3. 展示默认、悬停、焦点、按下、禁用、加载、空、错误和权限不足状态。
4. 验证 1280px、1440px 和 1680px 桌面视口下的结构行为，不验收手机或平板布局。
5. 验证键盘操作、对比度、减少动效与屏幕阅读器名称。
6. 涉及 AI 或自动投放时，明确来源、模型、费用、风险、审批和审计信息。
7. 使用真实业务数据和真实长度文案评审，不以理想化占位内容作为验收依据。

本文件是 cookies 视觉实现的唯一基线。新增视觉语言必须先更新此文件，再进入组件和页面实现。
