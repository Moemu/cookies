# Tasks

- [x] Task 1: 建立 MVP 服务端基础与持久化。实现本地可运行的 API 服务、数据模型、文件或嵌入式数据库持久化、统一错误格式和健康检查。
  - [x] SubTask 1.1: 定义 Project、Artifact、GenerationJob、ChangeSet、AuditEvent 的服务端 Schema 与状态转换。
  - [x] SubTask 1.2: 实现项目、产物、任务、ChangeSet 与审计记录的 API 和持久化仓储。
  - [x] SubTask 1.3: 为状态转换、数据恢复和错误边界添加后端测试。

- [x] Task 2: 接入受保护的方舟 Provider Gateway。实现文本生成与图片/视频异步生成适配，统一读取环境变量中的凭据并映射指定模型。
  - [x] SubTask 2.1: 添加 `.env.example`、模型目录和服务端配置校验，不提交真实密钥。
  - [x] SubTask 2.2: 实现策略文本生成、媒体任务创建、查询、取消和标准化错误处理。
  - [x] SubTask 2.3: 为无密钥、Provider 失败、任务状态和密钥不泄露添加契约测试。

- [x] Task 3: 将前端演示主链路切换到真实 API。保留现有视觉质量，使用 API 驱动项目恢复、Brief 生成、创意任务和任务状态展示。
  - [x] SubTask 3.1: 增加类型安全的 API Client，并将 ProjectContext 从静态内存数据迁移到 API 数据。
  - [x] SubTask 3.2: 改造策略和创意工作区，支持输入需求、生成/确认 Brief、选择图片或视频、轮询生成结果、失败恢复与 AI 血缘展示。
  - [x] SubTask 3.3: 移除浏览器端 API Key 保存与伪校验；将模型页改为只读的能力与健康状态。
  - [x] SubTask 3.4: 为核心用户交互增加前端测试或等价的可重复浏览器验证。

- [x] Task 4: 实现受控投放模拟与审计体验。将 ChangeSet 预检、审批、模拟执行和回滚迁移到服务端状态机，并明确不执行真实广告平台写入。
  - [x] SubTask 4.1: 实现 ChangeSet 预检规则、审批、模拟执行、回滚及审计 API。
  - [x] SubTask 4.2: 改造投放计划和审批页面，展示预检结果、模拟执行证据、权限提示与回滚结果。
  - [x] SubTask 4.3: 添加状态机与不可越过预检的测试。

- [x] Task 5: 打磨路演入口、文档与质量门禁。提供可复演的预置项目、清晰的步骤引导、真实运行说明，并完成构建与端到端验证。
  - [x] SubTask 5.1: 添加预置路演项目、主路径引导和 Provider 未配置时的可理解降级状态。
  - [x] SubTask 5.2: 更新 README 与环境说明，正确描述 MVP 范围、启动方式、方舟配置和“模拟投放”边界。
  - [x] SubTask 5.3: 执行前端构建、后端测试、核心 API 冒烟和浏览器主路径验证，清除调试代码与临时文件。

- [x] Task 6: 收敛前端主链路到服务端持久化状态。
  - [x] SubTask 6.1: 将 ProjectContext 中的产物推进、产物编辑、ChangeSet 创建、审批状态更新和回滚操作替换为对应 API 调用；仅在服务端成功响应后更新界面状态，并在刷新后恢复。
  - [x] SubTask 6.2: 移除关键演示路径对 initialProjects、固定时间戳和内存 ChangeSet 的回退写入；服务不可用时保留用户输入并展示可恢复错误，不得伪造已保存或已审批状态。
  - [x] SubTask 6.3: 添加前端可重复验证，覆盖 Brief 确认、创意生成入口、ChangeSet 创建/审批/回滚后的刷新恢复。

- [x] Task 7: 完成真实媒体任务同步与 Brief 前置校验。
  - [x] SubTask 7.1: 在 Provider Adapter 和 GenerationService 中实现图片、视频任务的服务端查询/同步，将上游排队、运行、成功、失败和取消状态持久化为 GenerationJob，成功时保存稳定资产。
  - [x] SubTask 7.2: 禁止未关联已确认 Brief 的图片或视频生成请求；前端在缺少已确认 Brief 时禁用入口并说明修复方式。
  - [x] SubTask 7.3: 为上游状态轮询、失败诊断、取消和 Brief 前置条件补充 API 契约测试及浏览器验证。

- [x] Task 8: 封闭 ChangeSet 状态机绕过路径。
  - [x] SubTask 8.1: 移除或限制通用 PATCH ChangeSet 状态接口，使预检、审批、模拟执行和回滚只能通过受控命令端点推进。
  - [x] SubTask 8.2: 在服务端对审批和执行重复验证已确认 Brief、已就绪创意和正数预算边界，拒绝任何直接将 draft 置为 preflight_passed 或执行态的请求。
  - [x] SubTask 8.3: 更新测试，显式断言直接 PATCH 不能绕过预检，并覆盖无效输入、未授权审批与刷新后的审计证据。

- [x] Task 9: 修复既有持久化存储下预置路演项目的可复演性。
  - [x] SubTask 9.1: 当本地存储已有非路演项目时，确保启动后仍存在完整的预置路演项目，包含已确认 Brief、就绪创意、预检通过的 ChangeSet 和审计记录。
  - [x] SubTask 9.2: 增加启动后的 API 冒烟验证，覆盖“需求 -> 策略 -> 创意 -> 审批投放模拟 -> 审计”所需的预置数据。
  - [x] SubTask 9.3: 验证已有用户项目不会被预置路演数据初始化覆盖，并在刷新后保持可恢复。

- [x] Task 10: 修复创意产物状态在前端的映射与验收展示。
  - [x] SubTask 10.1: 将服务端 `ready` 产物和 `succeeded` 媒体任务映射为完成态，而非“制作中”；保留排队、运行、失败和取消的可辨识状态。
  - [x] SubTask 10.2: 添加可重复验证，覆盖图片和视频生成任务成功后刷新恢复的状态、AI 标识、模型与生成时间展示。

- [x] Task 11: 持久化前端示例运营数据并补全跨页面数据链路。将当前前端 mock 中可变的项目工作项、证据、活动、指标趋势、投放诊断与建议动作迁移到服务端本地持久化仓储，作为幂等的路演示例数据。
  - [x] SubTask 11.1: 扩展服务端领域模型、文件仓储和 API，支持按 Project 查询示例运营数据，并确保记录包含稳定 ID、归属、时间、状态及必要的展示字段。
  - [x] SubTask 11.2: 将 `src/data/mock.ts` 与 `src/data/projects.ts` 中的业务示例记录收敛为幂等种子数据；保留纯视觉配置、交互文案、素材模板和静态图片引用在前端，不将其伪装为运行时业务数据。
  - [x] SubTask 11.3: 改造 API Client、ProjectContext 与仪表盘/工作区/投放/审计相关页面，使其只读取当前 Project 的服务端示例记录；API 不可用时显示可恢复错误，不得回退到静态 mock 声称数据已保存。
  - [x] SubTask 11.4: 将项目阶段、进度、预算、状态与负责人等项目级运行信息纳入服务端模型或由服务端资源确定性推导，消除前端基于固定值的跨页不一致。
  - [x] SubTask 11.5: 添加服务端持久化、种子幂等、Project 隔离与 API 契约测试，并补充前端构建及主路径验证，确认刷新或重启后示例项目仍可完整串通需求、策略、创意、洞察、审批投放与审计。

- [x] Task 12: 修复项目运行信息的前端硬编码映射。将 API 的 `runtime` 字段纳入前端类型和 `ProjectContext` 映射，移除固定阶段、进度、预算、状态与负责人默认值，并验证路演项目与新建项目在刷新后保持服务端一致。
  - [x] SubTask 12.1: 扩展 `ApiProject` 和前端项目类型，完整映射服务端 `runtime`，对历史项目采用明确且可识别的服务端默认值。
  - [x] SubTask 12.2: 更新项目首页、仪表盘和管理页的运行字段消费，确保不再显示与服务端不一致的固定预算、进度或状态。
  - [x] SubTask 12.3: 重新执行服务端测试、前端构建与运行数据验收，覆盖刷新恢复和不同项目隔离。

- [x] Task 13: 基于现有路演截图优化桌面端关键工作流体验。围绕项目总览、创意工作区、投后分析和项目管理四个截图页面，建立“主任务优先、结论先于明细、状态可追溯、下一步明确”的连续体验，同时保持真实 API 数据和模拟投放边界。
  - [x] SubTask 13.1: 重构项目总览与项目管理的行动层级。将当前阶段、阻塞项、下一步操作和审计/预算上下文组织为清晰的主工作区与辅助区，避免同权重统计卡片和流程卡片分散注意力。
  - [x] SubTask 13.2: 收敛视频创作工作区。突出中央创作预览和当前镜头，压缩左侧镜头清单与右侧提示词/生成设置的视觉权重，补齐镜头选择、生成状态、不可用操作和键盘焦点反馈。
  - [x] SubTask 13.3: 重组投后分析的信息叙事。首屏优先呈现“发生什么、原因和推荐动作”的可读结论及核心趋势，保留筛选、指标口径、广告明细和验证入口作为下钻，而非并列争夺主视线。
  - [x] SubTask 13.4: 统一关键状态、空/错误/加载反馈及桌面可访问性。为上述页面补齐语义化状态、可见焦点、ARIA 名称、减少动效处理，并在 1280px、1440px、1680px 保持无关键操作遮挡。
  - [x] SubTask 13.5: 添加或更新针对关键用户动作的前端测试与可重复浏览器验证，覆盖项目主路径、创意镜头选择、投后建议动作、刷新恢复以及无 API 回退提示。

- [x] Task 14: 修复体验优化中发现的模拟数据与非持久化生成绕过。确保投后分析和前贴分镜的展示、动作与恢复都由当前 Project 的服务端资源驱动，不能将固定前端数据或定时器结果表达为已保存业务结论或创意资产。
- [x] SubTask 14.1: 将投后分析的趋势、原因、建议动作和复盘输入改为读取当前 Project 的服务端运营记录；无匹配数据时展示可恢复空态，筛选和刷新只反映真实服务端数据。
  - [x] SubTask 14.2: 将前贴分镜生成改为创建、查询并呈现服务端生成任务；仅在任务成功且产物持久化后允许加入素材箱，并处理未配置、失败、取消和刷新恢复。
  - [x] SubTask 14.3: 将端到端回归改为启动隔离的真实 API 与临时持久化仓储，验证刷新与项目隔离；禁止以全量路由 mock 代替服务端集成验证。

- [x] Task 15: 封闭前贴视频任务和素材箱的持久化边界。确保前贴生成、重试与素材展示均以当前 Project 内服务端持久化资源为唯一事实来源，消除跨前贴复用、旧成功态残留和固定业务视频绕过。
  - [x] SubTask 15.1: 为前贴视频 GenerationJob 与 Artifact 持久化并校验前贴类型和用途标识；创建、轮询、刷新恢复和素材选择均按当前 Project、前贴类型/用途隔离，禁止将其他前贴或用途的任务状态、成功视频或失败信息恢复到当前前贴。
  - [x] SubTask 15.2: 修复前贴视频重试状态机：重新创建请求成功受理后，立即清空该前贴旧任务的成功可用态、预览 URL 与“可加入素材箱”入口，仅允许新任务成功并持久化新 Artifact 后恢复可用；对创建失败、Provider 拒绝和网络错误保留明确可重试错误，不得展示旧成功视频为本次结果。
  - [x] SubTask 15.3: 改造素材箱数据源为当前 Project 服务端持久化 Artifact 查询结果，仅展示成功且归属明确的资产；移除固定业务生成视频、前端本地注入视频和任何绕过任务成功/资产持久化校验的加入逻辑，并为无资产、加载失败和跨项目资产提供可理解状态。
  - [x] SubTask 15.4: 补充真实 API E2E 与回归验证：使用隔离的临时持久化仓储和至少两个 Project，验证不同前贴类型/用途的任务及刷新恢复互不串扰、成功后重试不会复用旧可用态、素材箱仅显示持久化资产，并覆盖创建失败、任务失败、刷新及项目切换；禁止以全量路由 mock 替代上述服务端集成断言。

- [x] Task 16: 收敛主创意聚合、资产体验页与 E2E 数据准备的持久化事实来源。确保当前 Project 的创意、资产结论和端到端验收均不依赖前贴资产、固定前端业务数据或用例执行顺序。
  - [x] SubTask 16.1: 改造 `ProjectContext` 的主创意聚合逻辑，仅将当前 Project 中符合主创意用途的持久化 Artifact 与任务结果纳入主创意状态；按 `purpose`、`prerollType` 明确排除所有前贴资产，防止其被误用为主创意预览、选择项或完成态依据。
  - [x] SubTask 16.2: 收紧 ChangeSet 的创意选择、预检与执行输入：仅接受当前 Project 内已持久化且符合主创意用途的 Artifact，按 `purpose`、`prerollType` 排除前贴资产；对缺少合格主创意、跨项目资产或前贴资产返回明确可恢复错误，不得以任何前贴资源满足投放前置条件。
  - [x] SubTask 16.3: 改造 `AssetExperiencePage`，移除固定业务资产、固定 CTR 和 AI 生成结论；页面仅读取当前 Project 已持久化的 Artifact 及其可追溯元数据，或在无匹配资产、加载失败和未生成结论时展示明确空态/错误态，不得将静态前端数据表达为当前项目结果。
  - [x] SubTask 16.4: 补充验证与真实 API E2E：为主创意聚合、ChangeSet 前贴排除和资产体验页空态/持久化展示添加针对性测试；每个 E2E 用例独立创建所需 Project、Brief、Artifact、任务与运营数据，并使用隔离临时仓储或等价机制清理数据，禁止依赖其他用例的执行顺序、种子遗留数据或共享状态。

- [x] Task 17: 加固文件仓储并发持久化与桌面端前贴创意验收。串行化文件仓储写入以消除并发 `tmp`/`rename` 竞争，并以隔离真实 API E2E 验证取消后素材箱门禁及桌面视口布局。
  - [x] SubTask 17.1: 为文件仓储的 `mutate` 操作引入仓储实例级串行队列或等价互斥机制，保证读取、变更、写入临时文件和原子 `rename` 在同一持久化文件上按调用顺序完成；保留现有错误传播与崩溃恢复语义，不得以共享固定临时文件名造成竞争。
  - [x] SubTask 17.2: 添加并发持久化回归测试，同时发起多个创建、更新或状态推进操作，断言全部成功变更在重新加载后均存在、持久化文件可解析且不残留竞争产生的临时文件或丢失写入。
  - [x] SubTask 17.3: 添加真实 API 前贴 E2E：在隔离临时仓储中创建前贴任务，取消任务并刷新页面，断言取消态由服务端恢复，旧预览与“加入素材箱”入口不可用，素材箱不展示该取消任务的资产；禁止以全量路由 mock 替代服务端集成断言。
  - [x] SubTask 17.4: 添加 1280px、1440px、1680px 桌面视口 E2E，断言页面无横向滚动溢出，且前贴工作区、素材箱及关键生成、取消、加入素材箱控件均在可视区域内、可操作且未被遮挡。
  - [x] SubTask 17.5: 执行文件仓储并发测试、真实 API E2E 和前端构建，记录并修复任何跨用例持久化污染、取消后状态回退或桌面视口溢出问题。

- [x] Task 18: 增加前贴工作区与素材剪辑页的初始视口 E2E 验收。在 1280px、1440px、1680px 桌面视口中，于页面加载后不进行预滚动即断言素材箱和关键控件可见、可点、位于视口边界内且页面无横向溢出。
  - [x] SubTask 18.1: 添加独立 E2E 用例，分别覆盖前贴工作区与素材剪辑页的初始视口；在每个视口下验证素材箱以及关键生成、取消、加入素材箱和剪辑操作控件不被遮挡、可交互，并断言文档与页面容器均无横向滚动溢出。

- [x] Task 19: 建立短剧前贴可解释候选规划核心。实现不依赖 React、网络或 Provider 的纯 TypeScript 规划器，验证已审核故事上下文，稳定生成三至五个候选，并保证正片首句不会被逐字输出。
  - [x] SubTask 19.1: 定义故事上下文、候选、规划和选中候选生成输入的领域类型；将短剧类型、候选数量、已审核卖点、梗概长度和空白输入作为显式验证边界。
  - [x] SubTask 19.2: 实现冲突、反转、悬念和卖点衔接候选的确定性评分、证据、口播、画面意图及非逐字衔接语，并生成受控视频 Prompt。
  - [x] SubTask 19.3: 添加纯规划器单元测试，覆盖稳定排序、三至五项范围、缺失卖点、无效类型、首句不泄露和 Prompt 护栏。

- [x] Task 20: 将短剧前贴规划接入既有服务端任务与产物链路。新增 Project 和已确认 Brief 约束的规划 API，扩展短剧前贴生成请求，使服务端重建 Prompt 并把选择快照持久化到 Artifact。
  - [x] SubTask 20.1: 实现 `POST /api/short-drama-preroll-plans`，在返回候选前校验当前 Project、已就绪 Brief 与故事上下文，并沿用既有统一错误信封。
  - [x] SubTask 20.2: 扩展 `POST /api/generation/media` 的 `short_drama` 分支，要求有效规划和显式选中候选；忽略客户端原始 Prompt，并保留游戏、电商前贴的现有行为。
  - [x] SubTask 20.3: 在媒体生成完成时把不含 Provider URL、密钥及原始首句的规划版本、故事上下文、选中候选与服务端 Prompt 快照写入 Artifact，并添加 Project 隔离、Brief 门禁和持久化恢复契约测试。

- [x] Task 21: 改造短剧前贴工作区为人工候选选择流程。通过 API 获取规划结果，保留用户输入和失败恢复，要求选择候选后才允许创建短剧前贴任务，同时不改变游戏前贴与素材箱门禁。
  - [x] SubTask 21.1: 在类型安全 API Client 中定义规划 DTO、规划请求和带选中候选的短剧生成请求；禁止前端向短剧分支传递可编辑原始 Prompt。
  - [x] SubTask 21.2: 在短剧前贴工作区增加故事上下文表单、规划加载/错误状态和可访问的候选辅助面板，明确标识 AI 候选需要人工选择。
  - [x] SubTask 21.3: 将中央预览内容绑定当前选中候选，保持当前 Project/Brief/Provider 检查、任务轮询、取消、刷新恢复及“产物持久化后才能加入素材箱”门禁。
  - [x] SubTask 21.4: 调整样式，使候选面板为可收起的辅助区；在 1280px、1440px、1680px 桌面视口中不遮挡预览、候选选择或关键操作。

- [x] Task 22: 完成短剧前贴增量的全链路验收、文档和远程交付。更新能力说明，执行服务端、构建、真实 API E2E、空白检查和远程 CI 验证，仅提交本任务相关文件。
  - [x] SubTask 22.1: 添加真实 API E2E 或等价集成验证，覆盖已确认 Brief 下的候选规划、显式选择、生成、刷新恢复、跨 Project 拒绝以及游戏/电商前贴回归。
  - [x] SubTask 22.2: 更新 README，明确短剧前贴使用用户审核故事上下文与人工候选选择，不支持视频上传、VLM 理解、混剪、真实投放或前端凭据。
  - [x] SubTask 22.3: 执行 `npm run test:server`、`npm run check:server`、`npm run build`、相关 E2E、`git diff --check`，检查暂存内容不含敏感信息，提交并推送当前分支。
  - [x] SubTask 22.4: 推送后使用 `gh pr checks` 监测当前提交的必需检查；若失败，读取日志、修复根因、重跑本地检查并再次推送，直至检查通过。

- [ ] Task 23: 提交并推送当前范围内未交付变更: 当前工作区仍包含纳入本轮验证范围的未提交文件，远程 PR 检查仅覆盖 `HEAD`，不能证明这些变更已提交到远程代码仓库。

# Task Dependencies
- Task 2 依赖 Task 1 的服务端配置与统一错误基础。
- Task 3 依赖 Task 1 与 Task 2 的 API 契约；其前端 API Client 可与 Task 2 并行准备。
- Task 4 依赖 Task 1；其前端页面改造可与 Task 3 的策略/创意改造并行。
- Task 5 依赖 Task 1 至 Task 4 的核心主路径完成。
- Task 6 依赖 Task 1 的资源 API，Task 7 依赖 Task 2 的 Provider Gateway，Task 8 依赖 Task 4 的 ChangeSet 状态机。
- Task 11 依赖 Task 1 的持久化基础与 Task 3 的前端 API 数据恢复能力。
- Task 12 依赖 Task 11 的 Project runtime API 与运营数据迁移。
- Task 13 依赖 Task 3、Task 4、Task 11 和 Task 12 提供的真实 API 状态、投放模拟门禁与跨页面运行数据。
- Task 14 依赖 Task 7 的媒体任务状态机、Task 11 的运营数据 API 与 Task 13 的交互重构。
- Task 15 依赖 Task 7 的媒体任务与 Artifact 持久化、Task 14 的前贴服务端任务链路，以及 Task 14.3 的真实 API E2E 隔离基础。
- Task 16 依赖 Task 11 的 Project 运营数据 API、Task 14 的持久化投后分析链路与 Task 15 的前贴资产隔离和真实 API E2E 基础。
- Task 17 依赖 Task 1 的文件仓储基础、Task 15 的前贴任务与素材箱持久化边界，以及 Task 16.4 的隔离真实 API E2E 数据准备；其验证包括并发持久化回归、取消后刷新素材箱门禁、1280px/1440px/1680px 无横向溢出和关键控件可见性 E2E，以及前端构建。
- Task 18 依赖 Task 17 的前贴桌面视口 E2E 基础；其独立验收进一步要求前贴工作区与素材剪辑页在不预滚动的初始视口中完成可见性、可操作性、视口边界和无横向溢出断言。
- Task 19 可独立于既有前贴 UI 开始；Task 20 依赖 Task 19 的领域类型与纯规划器；Task 21 依赖 Task 20 的规划和受控生成 API；Task 22 依赖 Task 19 至 Task 21。
