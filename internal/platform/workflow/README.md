# Workflow platform module

Owner: Platform team.

This module will persist and execute generic Jobs, approvals, notifications,
and reliable event delivery. `contract.Job` is available now; queue and worker
implementations are deliberately deferred until the first Provider Job is
implemented.
