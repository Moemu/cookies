# cookies web shell

The React shell owns only global navigation, project context switching, global
tasks, approvals, notifications, and the management entry. A vertical system
owns its own routes, side navigation, page state, and business UI after it is
mounted here.

```bash
npm install
npm run dev
npm run check
```
